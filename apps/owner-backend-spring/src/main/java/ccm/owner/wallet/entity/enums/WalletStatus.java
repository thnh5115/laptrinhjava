package ccm.owner.wallet.entity.enums;

/**
 * Enum - Wallet status states
 */
public enum WalletStatus {
    ACTIVE,   // Wallet is active and can be used
    FROZEN    // Wallet is frozen (e.g., due to suspicious activity)
}