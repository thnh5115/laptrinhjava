# ---------------------------------------------------------------------------
# CVA Backend - Development Environment Template
# Copy to `.env` or export the variables below before running the service.
# ---------------------------------------------------------------------------

# Database connection (defaults align with infra/docker compose stack)
DB_URL=jdbc:mysql://localhost:3306/carbon_cva
DB_USERNAME=root
DB_PASSWORD=123456

# Frontend origins and dev credentials (used by Basic Auth in dev profile)
APP_FRONTEND_ORIGIN=http://localhost:3000
APP_SECURITY_DEV_USERNAME=cva_officer
APP_SECURITY_DEV_PASSWORD=password123

# Optional integrations -----------------------------------------------------
# Uncomment and adjust when the Admin Audit service is reachable.
# APP_AUDIT_BASE_URL=http://localhost:8080
# APP_AUDIT_RECORD_PATH=/api/admin/audit/logs
# APP_AUDIT_CONNECT_TIMEOUT=2s
# APP_AUDIT_READ_TIMEOUT=5s

# Configure when the Owner wallet service is available.
# APP_WALLET_BASE_URL=http://localhost:8084
# APP_WALLET_CREDIT_PATH=/api/owner/wallets/{ownerId}/credit
# APP_WALLET_CONNECT_TIMEOUT=2s
# APP_WALLET_READ_TIMEOUT=5s

# Rate limiting knobs (keep defaults unless load-testing)
# APP_RATE_LIMIT_APPROVE_LIMIT=10
# APP_RATE_LIMIT_APPROVE_WINDOW=1m
# APP_RATE_LIMIT_REJECT_LIMIT=10
# APP_RATE_LIMIT_REJECT_WINDOW=1m
# APP_RATE_LIMIT_REPORT_LIMIT=6
# APP_RATE_LIMIT_REPORT_WINDOW=1m
# APP_RATE_LIMIT_DEFAULT_LIMIT=30
# APP_RATE_LIMIT_DEFAULT_WINDOW=1m
